<!DOCTYPE html>
<html dir="{{COMMON.META.DIRECTION}}" lang="{{COMMON.META.LANGUAGE}}">
<head>
	<meta charset="UTF-8" />
	<title>{{COMMON.META.TITLE}}</title>

	<meta name="description" content="{{COMMON.META.DESCRIPTION}}" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />
	<link rel="icon" href="{{$image COMMON.META.FAVICON}}" />

	<meta property="og:title" content="{{COMMON.META.TITLE}}" />
	<meta property="og:description" content="{{COMMON.META.DESCRIPTION}}" />
	<meta property="og:image" content="{{$image COMMON.META.OG_IMAGE}}" />

	{{{$css 'assets/style.css'}}}

	{{#if COMMON.META.PRE_HTML}}
		{{{$html COMMON.META.PRE_HTML}}}
	{{/if}}

	<script src="https://cdn.tailwindcss.com"></script>
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Source+Sans+Pro:wght@400;700&display=swap" rel="stylesheet"> 

	<style>
		.gradient {
			background: linear-gradient(90deg, {{COMMON.THEME.COLOR_A}} 0%, {{COMMON.THEME.COLOR_B}} 100%);
		}
	</style>
</head>
<body class="leading-normal tracking-normal text-white gradient" style="font-family: 'Source Sans Pro', sans-serif;">

<!--Nav-->
<nav id="header" class="fixed w-full z-30 top-0 text-white">

	<div class="w-full container mx-auto flex flex-wrap items-center justify-between mt-0 py-2">
			
		<div class="pl-4 flex items-center mr-1">
			<a class="toggleColour text-white no-underline hover:no-underline font-['Pacifico'] text-2xl lg:text-2xl" href="#">
				<img src="{{{$image COMMON.GENERAL.SITE_LOGO}}}" class="h-10 fill-current inline" alt="" />
				<span class="pt-1 inline-block absolute pl-2">{{COMMON.GENERAL.SITE_NAME}}</span>
			</a>
		</div>

		{{#if LINKS.LINKS.LINKS}}
		<div class="block lg:hidden pr-4">
			<button id="nav-toggle" class="flex items-center p-1 hover:text-gray-900" style="color: {{COMMON.THEME.COLOR_A}};">
				<svg class="fill-current h-6 w-6" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
			</button>
		</div>
		{{/if}}

		{{#if LINKS.LINKS.LINKS}}
		<div class="w-full flex-grow lg:flex lg:items-center lg:w-auto hidden lg:block mt-2 lg:mt-0 bg-white lg:bg-transparent text-black p-4 lg:p-0 z-20" id="nav-content">
			<ul class="list-reset lg:flex justify-end flex-1 items-center">
				{{#each LINKS.LINKS.LINKS}}
				{{#if HEADER}}
					<li class="mr-3">
						<a href="{{URL}}"{{#if OPEN_IN_NEW_WINDOW}} target="_blank"{{/if}} 
							class="inline-block text-black no-underline hover:text-gray-800 hover:text-underline py-2 px-4">
							{{TITLE}}
						</a>
					</li>
				{{/if}}
				{{/each}}
			</ul>
		</div>
		{{/if}}
	</div>
	
	<hr class="border-b border-gray-100 opacity-25 my-0 py-0" />
</nav>